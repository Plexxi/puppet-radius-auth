#
# This file is managed by Puppet.
# Any changes will likely be overwritten
#
# server[:port]	shared_secret      timeout (s)

<% if @server -%>
<% Array(@server).each do |serv| -%>
<%= serv['addr'] -%><%= serv['port'] ? ':'+"serv['port']" : '' -%>    <%= serv['secret'] -%>    <%= serv['timeout'] ? serv['timeout'] : '2' %>
<% end -%>
<%end -%>
